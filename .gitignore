# Build outputs
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo
*.userprefs

# NuGet
*.nupkg
packages/
*.lock.json

# OS
.DS_Store
Thumbs.db

# OASIS 연결 설정 (암호 포함 — 배포 시 별도 배치, oasis.config.sample만 커밋)
oasis.config

# 일괄 갱신 설정 (개인 PC 경로 포함 — batch.config.sample만 커밋)
batch.config
